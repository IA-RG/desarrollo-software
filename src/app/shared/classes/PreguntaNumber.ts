import { Pregunta } from "./Pregunta";
/*
export class PreguntaNumber extends Pregunta<number>{
    private _min : number;
    private _max : number;
    private _step : number;
    constructor(
        options: {
          value?: number;
          key?: string;
          label?: string;
          required?: boolean;
          order?: number;
          controlType?: string;
          type?: string;
          disabled?: boolean;
          options?: { key: string; value: string }[];
        } = {},
        minimo : number, max : number, step : number
      ) {
        super(options);
        options.controlType = 'number';
        this._min = minimo;
        this._max = max;
        this._step = step;
      }
      
      public get minimo() : number {
          return this._min;
      }
      
      
      public get maximo() : number {
          return this._max
      }
      
      public get step() : number {
          return this._step;
      }
      
      
}
*/